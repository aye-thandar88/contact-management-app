export interface Contact {
  id?: number;
  firstName: string;
  lastName: string;
  email: string;
  role: string;
  gender: string;
  isActive: boolean;
  newsletter: boolean;
  birthDate: string;
}
